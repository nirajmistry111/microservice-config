spring:
  application:
    name: Production
eureka:
  instance:
    prefer-ip-address: false
  client:
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/       
